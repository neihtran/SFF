import { Controller } from '@nestjs/common';

/**
 * Reserved cho REST endpoint bổ sung (vd: /chat-gateway/online).
 * Hiện tại không cần thiết — để placeholder cho sau này.
 */
@Controller('chat-gateway')
export class ChatGatewayController {}